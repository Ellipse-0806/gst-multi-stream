mod settings;

use settings::AppConfig;

pub fn load() {
    let config = AppConfig::new();
    println!("{:#?}", config);
}

pub fn add(left: usize, right: usize) -> usize {
    left + right
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
}
